pipeline {
    agent any

    stages {
        stage ('test') {
            steps {
                echo 'We are testing
            }
        }
    }
    post {
        always {
            echo 'our code ran'
        }
    }
}