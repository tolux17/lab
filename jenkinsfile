pipeline {
    agent aws

    stages {
        stage ('test') {
            steps {
                echo 'We are testing'
            }
        }
    }
    post {
        always {
            echo 'our code ran'
        }
        success {
            echo 'our code was successful'
        }
        failure {
            echo 'our code failed'
        }
    }
}